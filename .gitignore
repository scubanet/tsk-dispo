node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/
coverage/
playwright-report/
test-results/
supabase/.branches/
supabase/.temp/
.turbo/

# Apple Push Notification secrets — NEVER commit
apps/Apple/
*.p8

# Vite dev-server build artefacts (transient)
**/vite.config.ts.timestamp-*

# Snapshot-Tarballs im Repo-Root (atoll-*, cd-*) — Tag-Archive, gehören nicht ins Repo
/atoll-*.tar.gz
/cd-*.tar.gz

# Lokale DB-Backups
/backup-*.sql

~$*
