// apps/web/src/screens/contacts/sidebar/sections/TagsSection.tsx
//
// STUB für Phase G Phase 3 Task 5 — wird in Task 9 ersetzt durch die echte
// Section (Tag-Pillen + Add-Tag).
import type { ContactWithProperties } from '@/types/contactProperties'

interface Props {
  contact: ContactWithProperties
}

export function TagsSection({ contact: _contact }: Props) {
  return (
    <div data-testid="section-stub-tags" style={{ padding: 8, color: '#888', fontSize: 12 }}>
      TagsSection-Stub (Task 9)
    </div>
  )
}
