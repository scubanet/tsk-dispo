// apps/web/src/screens/contacts/sidebar/sections/RolesStatusSection.tsx
//
// STUB für Phase G Phase 3 Task 5 — wird in Task 7 ersetzt durch die echte
// Section (Roles/Status mit Sidecar-Anzeige).
import type { ContactWithProperties } from '@/types/contactProperties'

interface Props {
  contact: ContactWithProperties
}

export function RolesStatusSection({ contact: _contact }: Props) {
  return (
    <div data-testid="section-stub-roles" style={{ padding: 8, color: '#888', fontSize: 12 }}>
      RolesStatusSection-Stub (Task 7)
    </div>
  )
}
