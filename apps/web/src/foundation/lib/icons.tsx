// Real implementation lives in icons.ts (uses React.createElement, not JSX,
// so a .ts extension is sufficient). This file is a stub kept only because
// the original .tsx file cannot be deleted from the workspace.
export {} // see icons.ts
